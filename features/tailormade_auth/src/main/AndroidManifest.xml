<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.future.tailormade_auth">

    <application android:theme="@style/Theme.TailorMade">

        <activity android:name=".feature.signIn.view.SignInActivity">
            <intent-filter>
                <action android:name="com.future.tailormade.signIn.open" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>